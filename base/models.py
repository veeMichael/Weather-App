from django.db import models

# Create your models here.


class City(models.Model):
    # can use this if we want to store data
    # don't need to because we only want the current weather.
    pass
